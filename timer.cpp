#include "timer.h"

// This is empty at the moment because the timer implementation is trivial enough to be done in the header
// However, if you need to add more complex functionality or member functions in the future, you can do so here.